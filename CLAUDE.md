# 초하루 (Chohyaru)

매달 첫날, 지난달에 내가 쓴 일기로 만든 편지가 도착하는 일기 앱.
2026 AI 해커톤 출품작 — 개발 마감 8/2, 발표 8/3.

**한 줄**: 내가 나한테 보낸 편지를, AI가 한 달 뒤에 배달한다.

## 이 프로젝트에서 가장 중요한 것

**위로의 저작권은 사용자에게 있다.**
AI는 위로를 만들지 않는다. 사용자가 이미 써둔 것을 찾아 되돌려줄 뿐이다.
기능을 추가할지 말지 고민되면 이렇게 묻는다 — *이게 사용자의 문장을 대신하는가, 되돌려주는가?*

## 문서

| 파일 | 내용 |
| --- | --- |
| `docs/plan.md` | 기획서. 기능 우선순위, 화면 흐름, AI 파이프라인, 일정 |
| `docs/prototype.html` | 룩과 화면 흐름의 정답지. 6화면 + 다크모드 |
| `.claude/skills/chohyaru-design/SKILL.md` | 디자인 시스템. **UI 작업 전 반드시 읽을 것** |

UI·화면·컴포넌트·편지 문안 작업은 무조건 디자인 스킬을 먼저 읽는다. "디자인"이라는 단어가 없어도 마찬가지다.

## 기술 스택

- React Native (Expo) — `npx expo start`, 폰의 Expo Go로 QR 스캔
- 저장: AsyncStorage (기기 로컬)
- 인증: `expo-local-authentication` (기기 등록된 지문/Face ID) + PIN fallback
- OCR: Upstage Document Parse
- 분석·편지 조립: LLM

**쓰지 않는 것**: 백엔드 서버, 로그인·회원가입, 벡터 DB, 외부 UI 라이브러리

한 달치 문장은 많아야 수백 개다. 벡터 DB를 붙이지 말 것 — 인메모리로 충분하고 시간만 잡아먹는다.

## 아키텍처 메모

편지 생성 파이프라인은 5단계다.

```
지난달 일기 → ① 전처리 → ② 태깅 → ③ 신호 추출 → ④ 조립 → ⑤ 검증
```

- ③에서 뽑는 신호: 반복된 감정 / 사라진 걱정 / 말 안 한 노력
- ④는 LLM, **⑤는 반드시 순수 코드**로 짤 것. 인용 문장이 원본 일기에 실제로 존재하는지 문자열 대조하고, 불일치하면 제거 후 재조립한다
- 각 인용에는 원본 날짜를 붙여 저장한다 → 편지에서 탭하면 그날 일기로 이동

⑤를 LLM에게 맡기면 이 서비스의 근거가 사라진다.

## 코드 규칙

- 색·폰트·간격은 전부 `theme.ts`의 토큰에서 가져온다. 컴포넌트에 하드코딩된 hex 금지
- 다크모드는 처음부터 함께 짠다. 나중에 얹으면 하드코딩된 색이 전부 터진다
- 테마 컨테이너와 각 화면에 색을 명시적으로 선언할 것 (CSS 변수는 계산된 값으로 상속되어, 바깥에서 정해진 글자색이 그대로 따라온다)
- 날짜 키는 `YYYY-MM-DD` 문자열로 통일

## 절대 하지 말 것

- **스트릭·달성률·연속 기록·레벨·배지** — 게이미피케이션 일체 금지. 빠진 날을 실패로 만드는 순간 이 앱이 비판하는 대상이 된다
- **캘린더에서 안 쓴 날을 표시하는 것** — 빈 칸이 죄책감의 격자가 된다. 쓴 날에만 점을 찍는다
- **편지에 조언·다음 달 목표 제안** — 코치가 되는 순간 정체성이 무너진다
- **편지에 통계·퍼센트·집계 어투** — "아홉 번 썼다"가 아니라 "언제 괜찮아졌는지 모르겠어"
- **편지 공유 기능** — 공유가 붙으면 일기 자체가 검열된다
- **일기를 서버로 보내는 것** — 기기 로컬이 원칙이다

## 편지를 쓰거나 고칠 때

화자는 AI가 아니라 **지난달의 나**다. 반말, 인사로 시작해 서명으로 끝난다.
가장 흔한 실패는 요약 보고체다. 편지는 세지 않는다 — 회상하고, 감상을 붙이고, 한마디 남긴다.
힘든 얘기만 담지 말 것. 좋았던 날, 웃겼던 날, 별거 없던 날을 반드시 섞는다.

자세한 규칙과 예시 편지 전문은 디자인 스킬 8절에 있다.

## 데모 원칙

발표 시연은 **팀원의 진짜 한 달 일기**로 한다. 가짜 데이터로 만든 편지는 티가 나고, 그 순간 설득력이 통째로 날아간다.

시연 경로 — 이것만 매끄러우면 된다.
> 지문 인증 → 캘린더에서 지난달 확인 → 편지 자동 등장 → 봉투 열기 → 편지 읽기 → 문장 탭 → 그날 일기로 이동

**데모 모드 토글을 반드시 만들 것.** 편지는 매달 1일에만 뜨는데 발표는 8/3이다. 날짜를 강제로 바꾸는 숨김 버튼이 없으면 발표 당일에 편지를 못 띄운다.